/*
    Find out how we can join two or more strings together.

    A:
    Strings can be concatenated in two ways. The first is to use "+." The second is to 
    use a string literal (e.g. `${string1}${string2}`)

    Correction:
    You can also use the String.concat(string2, string3) method.
*/

