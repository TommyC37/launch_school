/*
    How many arguments does the Array.prototype.join() method expect?
    What happens if you call it with less or more arguments?

    Array.prototype.join() expects a "separator" parameter. If there is no parameter passed, then it joins all the
    array elements and separates them with a comma. If multiple parameters are passed, it ignores any paramter after
    the first.

*/