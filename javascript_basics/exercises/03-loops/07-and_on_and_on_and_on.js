/* The following code keeps looping forever (you can hit ctrl-c to stop it). Why
is that? Also modify it so that it stops after the first iteration.

The code continues infinitely because there is no stopping condition.

*/

for (let i = 0; i < 1; i += 1) {
  console.log('and on');
}