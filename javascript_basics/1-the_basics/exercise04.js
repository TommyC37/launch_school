console.log('5' + 10);

/* The above expression evaluates to 510 and 15 because of implicit type 
    coercion. JS converts 10 to '10' (i.e. converts from Number to String)
    and concatenates it to '5'
*/ 