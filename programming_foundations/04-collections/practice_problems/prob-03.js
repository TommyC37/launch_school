// The following code differs slightly from the above code (from previous problem).
// What is the return value of map in this case? Why?

[1, 2, 3].map(num => num * num);

// This will return [1, 4, 9] because we're using an arrow function (no return statement needed)