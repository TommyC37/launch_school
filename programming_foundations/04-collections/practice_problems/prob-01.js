// What is the return value of the filter method called below? Why?

[1, 2, 3].filter(num => 'hi');

// The return value is [1, 2, 3] because it will return "hi" for every num.
// Since the string is not empty, it is truthy.